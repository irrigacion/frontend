{
	"$schema": "https://turborepo.com/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", "build/**", ".expo/**", "web-build/**"]
		},
		"lint": {
			"outputs": []
		},
		"test": {
			"outputs": ["coverage/**"]
		}
	}
}
